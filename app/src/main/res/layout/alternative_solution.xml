<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/blue"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="@string/title"
        android:textColor="@color/darkgrey"
        android:textSize="30dp"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/list_slot_one"
        android:background="@color/lightgrey"
        android:layout_width="100dp"
        android:layout_height="300dp"
        android:scrollbars="none"
        android:layout_alignTop="@+id/list_slot_two"
        android:layout_toStartOf="@+id/list_slot_two" />
    <ListView
        android:id="@+id/list_slot_two"
        android:background="@color/lightgrey"
        android:layout_centerInParent="true"
        android:scrollbars="none"
        android:layout_width="100dp"
        android:layout_height="300dp" />
    <ListView
        android:id="@+id/list_slot_three"
        android:layout_toRightOf="@id/list_slot_two"
        android:background="@color/lightgrey"
        android:scrollbars="none"
        android:layout_centerVertical="true"
        android:layout_width="100dp"
        android:layout_height="300dp" />

    <Button
        android:id="@+id/play_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="play"
        android:layout_alignParentBottom="true"
        android:layout_toEndOf="@+id/list_slot_one" />

</RelativeLayout>
